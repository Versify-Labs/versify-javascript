/**
 * Versify API
 * Versify API
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Account from './Account';

/**
 * The ApiSearchResponse model module.
 * @module model/ApiSearchResponse
 * @version 1.0.0
 */
class ApiSearchResponse {
    /**
     * Constructs a new <code>ApiSearchResponse</code>.
     * @alias module:model/ApiSearchResponse
     */
    constructor() { 
        
        ApiSearchResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ApiSearchResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApiSearchResponse} obj Optional instance to populate.
     * @return {module:model/ApiSearchResponse} The populated <code>ApiSearchResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApiSearchResponse();

            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [Account]);
            }
            if (data.hasOwnProperty('has_more')) {
                obj['has_more'] = ApiClient.convertToType(data['has_more'], 'Boolean');
            }
            if (data.hasOwnProperty('object')) {
                obj['object'] = ApiClient.convertToType(data['object'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApiSearchResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApiSearchResponse</code>.
     */
    static validateJSON(data) {
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                Account.validateJsonObject(item);
            };
        }
        // ensure the json data is a string
        if (data['object'] && !(typeof data['object'] === 'string' || data['object'] instanceof String)) {
            throw new Error("Expected the field `object` to be a primitive type in the JSON string but got " + data['object']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}



/**
 * The number of items returned
 * @member {Number} count
 * @default 0
 */
ApiSearchResponse.prototype['count'] = 0;

/**
 * The list of items that match the filters and pagination parameters.
 * @member {Array.<module:model/Account>} data
 */
ApiSearchResponse.prototype['data'] = undefined;

/**
 * Whether there are more items to be returned
 * @member {Boolean} has_more
 * @default false
 */
ApiSearchResponse.prototype['has_more'] = false;

/**
 * The object type
 * @member {String} object
 * @default 'search_result'
 */
ApiSearchResponse.prototype['object'] = 'search_result';

/**
 * The URL of the search request
 * @member {String} url
 * @default '/v1/items/search'
 */
ApiSearchResponse.prototype['url'] = '/v1/items/search';






export default ApiSearchResponse;

